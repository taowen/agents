{
  "name": "@cloudflare/browser-agent-example",
  "description": "Browser-based AI agent example — LLM calls via proxy Worker, tool execution (just-bash) runs entirely client-side",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "start": "vite dev",
    "deploy": "vite build && wrangler deploy"
  },
  "dependencies": {
    "just-bash": "file:../../packages/just-bash"
  }
}

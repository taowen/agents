# LLM Provider: "openai-compatible" (default) or "google"
LLM_PROVIDER=openai-compatible

# API base URL (required for openai-compatible provider)
LLM_BASE_URL=https://api.openai.com/v1

# API key (required)
LLM_API_KEY=sk-xxx

# Model name (required)
LLM_MODEL=gpt-4o
